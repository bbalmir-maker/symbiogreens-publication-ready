// Copy this file to a deployment-only runtime config file if your host cannot inject env vars.
// Never place service-role keys or Resend keys in frontend code.
window.SYMBIO_SUPABASE_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
