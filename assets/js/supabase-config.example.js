const SUPABASE_URL = "https://YOUR_PROJECT.supabase.co";
const SUPABASE_ANON_KEY = "YOUR_PUBLIC_ANON_KEY";

// Replace these placeholders with your real Supabase project URL and public anon key.
// Never expose the Supabase service role key in front-end code.
// Enable Row Level Security before production use.
// The anon key is public only when RLS policies are configured correctly.
window.SYMBIOGREENS_SUPABASE_ENABLED = false;
